[home]
# Coordinates of your home — triggers "arriving" geofence automations
latitude  = <lat>
longitude = <long>

[away]
# Must be at least 500m from home to reliably trigger a "leave" geofence
latitude  = <lat>
longitude = <long>